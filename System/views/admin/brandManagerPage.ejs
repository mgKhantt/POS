<%- contentFor('style') %>
    <style>

    </style>

    <%- contentFor('body') %>
        <div class="container mt-5">
            <div class="card shadow p-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h2 class="card-title mb-0">Category Manager</h2>
                    <button class="btn btn-outline-secondary" onclick="window.history.back()">
                        <i class="fa fa-arrow-left"></i> Back
                    </button>
                </div>

                <!-- Create Brand -->
                <form action="/admin/products/create-product/brand-manager" method="POST" class="mb-4">
                    <div class="input-group">
                        <input type="text" class="form-control" name="name" placeholder="Enter new category name"
                            required />
                        <button class="btn btn-primary" type="submit">
                            <i class="fa fa-plus"></i> Add
                        </button>
                    </div>
                </form>

                <!-- Brand List -->
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>Brand Name</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% brands.forEach((brand, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= brand.name %>
                                </td>
                                <td>
                                        <button class="btn btn-sm btn-danger" onclick="deleteCategory('<%= brand._id %>', '<%= brand.name %>')">
                                            <i class="fa fa-trash"></i> Delete
                                        </button>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <script>
            async function deleteCategory(brandID, brandName) {
                Swal.fire({
                    title: "Are you sure?",
                    text: `${brandName} brand will be permanently deleted. `,
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Yes, delete it!"
                }).then( result => {
                    if(result.isConfirmed) {
                        fetch(`/admin/products/create-product/brand-manager/delete/${brandID}`,
                            {
                                method: 'DELETE',
                            }
                        )
                        .then(res => res.json())
                        .then(data => {
                            Swal.fire("Deleted!", `${brandName} brand has been deleted.`, "success")
                            .then(() => location.reload());
                        })
                        .catch(err => console.error(err));
                    }
                })
            }
        </script>